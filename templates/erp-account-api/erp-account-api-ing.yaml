apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: erp-account-api-ing
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.org/client-max-body-size: 50m
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.domainErpAccountAPI }}
      secretName: erp-account-api-cert
  rules:
    - host: {{ .Values.domainErpAccountAPI }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: promerp-{{ .Values.envAlias }}-erp-account-api
                port:
                  number: 80
