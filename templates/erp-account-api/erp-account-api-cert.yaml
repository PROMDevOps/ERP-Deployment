apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: erp-account-api-cert
spec:
  secretName: erp-account-api-cert
  dnsNames:
    - {{ .Values.domainErpAccountAPI }}
  issuerRef:
    name: lets-encrypt-issuer-prod
    kind: ClusterIssuer
    group: cert-manager.io
