apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: erp-account-ui-cert
spec:
  secretName: erp-account-ui-cert
  dnsNames:
    - {{ .Values.domainErpAccountUI }}
  issuerRef:
    name: lets-encrypt-issuer-prod
    kind: ClusterIssuer
    group: cert-manager.io
