apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: erp-ui-cert
spec:
  secretName: erp-ui-cert
  dnsNames:
    - {{ .Values.domainErpUI }}
  issuerRef:
    name: lets-encrypt-issuer-prod
    kind: ClusterIssuer
    group: cert-manager.io
